<UserControl x:Class="UI.Components.StudentsList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:UI.Components"
             xmlns:converters="clr-namespace:UI.Converters"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <converters:PasswordConverter x:Key="PasswordConverter"/>
    </UserControl.Resources>
    <Grid>
        <DataGrid Name="students" ItemsSource="{Binding}" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="#" IsReadOnly="True" Binding="{Binding Id}"></DataGridTextColumn>  
                <DataGridTextColumn Header="Names" IsReadOnly="True" Binding="{Binding Name}"></DataGridTextColumn>  
                <DataGridTextColumn Header="Password" IsReadOnly="True" Binding="{Binding Password, Converter={StaticResource PasswordConverter}}"></DataGridTextColumn>
                <DataGridTextColumn Header="Role" IsReadOnly="True" Binding="{Binding Role}"></DataGridTextColumn> 
                <DataGridTextColumn Header="Expires In" IsReadOnly="True" Binding="{Binding Expires}"></DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
