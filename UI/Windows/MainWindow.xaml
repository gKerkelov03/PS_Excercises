<Window x:Class="UI.Windows.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UI.Windows"
        xmlns:converters="clr-namespace:UI.Converters"
        mc:Ignorable="d"
        Title="User Management" Height="450" Width="800">
    <Window.Resources>
        <converters:PasswordConverter x:Key="PasswordConverter"/>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10">
            <Button Content="Add User" Command="{Binding AddUserCommand}" Margin="0,0,10,0" Padding="10,5"/>
            <Button Content="Edit User" Command="{Binding EditUserCommand}" Margin="0,0,10,0" Padding="10,5"/>
            <Button Content="Delete User" Command="{Binding DeleteUserCommand}" Margin="0,0,10,0" Padding="10,5"/>
            <Button Content="View Logs" Command="{Binding ViewLogsCommand}" Margin="0,0,10,0" Padding="10,5"/>
            <Button Content="Manage Disciplines" Command="{Binding ManageDisciplinesCommand}" Padding="10,5"/>
        </StackPanel>

        <DataGrid Grid.Row="1" 
                  ItemsSource="{Binding Users}" 
                  SelectedItem="{Binding SelectedUser}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  Margin="10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Username" Binding="{Binding Username}" Width="*"/>
                <DataGridTextColumn Header="Role" Binding="{Binding Role}" Width="*"/>
                <DataGridTextColumn Header="Faculty Number" Binding="{Binding FacultyNumber}" Width="*"/>
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
